package model.peripheral.peripherals;

import model.peripheral.Peripheral;

public class Printer extends Peripheral {
    public Printer(String connectionComputer, String brand) {
        super(connectionComputer, brand);
    }
}
